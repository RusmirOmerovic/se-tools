#!/usr/bin/env bash
# projectnew.sh – Neues Projekt (GitHub + Supabase via Vercel-Proxy, kein Key im Repo)
# Abhängigkeiten: git, curl, jq, (macOS) security
set -euo pipefail

# ========= Konfiguration =========
# Deine Vercel-App-Basis-URL (ohne Slash am Ende), z.B. https://frontend-se-cyan.vercel.app
: "${VERCEL_BASE:?Setze VERCEL_BASE, z.B. export VERCEL_BASE='https://frontend-se-cyan.vercel.app'}"

TEMPLATE_REPO="${TEMPLATE_REPO:-https://github.com/RusmirOmerovic/html-template-se.git}"
USERNAME="${USERNAME:-RusmirOmerovic}"
GITHUB_API="https://api.github.com"
KEYCHAIN_GH="se-tools-gh-token"

GREEN='\033[0;32m'; CYAN='\033[0;36m'; RED='\033[0;31m'; NC='\033[0m'

need(){ command -v "$1" >/dev/null 2>&1 || { echo -e "${RED}❌ fehlt: $1${NC}"; exit 1; }; }
need git; need curl; need jq
command -v security >/dev/null 2>&1 || echo -e "${CYAN}ℹ️ security (Keychain) nicht gefunden – GH-Token wird dann nur per Prompt gelesen.${NC}"

get_github_token() {
  if [[ -n "${GITHUB_TOKEN:-}" ]]; then
    echo -n "$GITHUB_TOKEN"
  else
    echo -e "${RED}❌ Kein GitHub-Token gefunden. Bitte zuerst 'gh auth login' ausführen.${NC}"
    exit 1
  fi
}

# ========= Eingaben prüfen =========
if [ $# -lt 1 ]; then echo -e "${RED}❌ Projektnamen angeben!${NC}"; exit 1; fi
PROJECT_NAME="$1"
ZIELORDNER="$PWD/$PROJECT_NAME"
[ -d "$ZIELORDNER" ] && { echo -e "${RED}❌ Ordner '$ZIELORDNER' existiert bereits.${NC}"; exit 1; }

# ========= GitHub-Repo aus Template erstellen =========
echo -e "${CYAN}→ GitHub Repo anlegen & pushen …${NC}"
GITHUB_TOKEN="$(get_github_token)"

EXIST_HTTP="$(curl -s -o /dev/null -w "%{http_code}" -H "Authorization: Bearer $GITHUB_TOKEN" \
  "$GITHUB_API/repos/$USERNAME/$PROJECT_NAME")"
if [ "$EXIST_HTTP" = "200" ]; then
  echo -e "${RED}❌ Repo '$USERNAME/$PROJECT_NAME' existiert bereits.${NC}"
  exit 1
fi

git clone "$TEMPLATE_REPO" "$ZIELORDNER"
cd "$ZIELORDNER"
rm -rf .git && git init
git config user.name  >/dev/null 2>&1 || git config user.name  "$USERNAME"
git config user.email >/dev/null 2>&1 || git config user.email "no-reply@users.noreply.github.com"
echo -e ".env\n.DS_Store\nnode_modules" > .gitignore
git add . && git commit -m "🆕 Neues Projekt: $PROJECT_NAME"

RESP_CODE="$(curl -s -o /dev/null -w "%{http_code}" \
  -H "Authorization: Bearer $GITHUB_TOKEN" \
  -H "Accept: application/vnd.github+json" \
  -H "Content-Type: application/json" \
  -d "{\"name\":\"$PROJECT_NAME\",\"private\":false}" \
  "$GITHUB_API/user/repos")"
if [ "$RESP_CODE" != "201" ]; then
  echo -e "${RED}❌ GitHub Repo-Erstellung fehlgeschlagen (HTTP $RESP_CODE).${NC}"; exit 1
fi

git remote add origin "https://github.com/$USERNAME/$PROJECT_NAME.git"
git branch -M main
git push -u origin main
REPO_URL="https://github.com/$USERNAME/$PROJECT_NAME"

# ========= Login über Vercel-API (kein Key lokal nötig) =========
echo -e "${CYAN}→ Login via Vercel-API …${NC}"
read -r -p "📧 Login E-Mail: " LOGIN_EMAIL

MAX_TRIES=3
TRY=1
ACCESS_TOKEN=""

while [ $TRY -le $MAX_TRIES ]; do
  # Passwort abfragen (bash + zsh kompatibel)
  if [ -n "${ZSH_VERSION-}" ]; then
    read -s "LOGIN_PASS?🔒 Passwort: "; echo
  else
    read -s -p "🔒 Passwort: " LOGIN_PASS; echo
  fi

  LOGIN_HTTP=$(curl -s -o /tmp/login.json -w "%{http_code}" \
    -X POST "$VERCEL_BASE/api/login" \
    -H "Content-Type: application/json" \
    -d "{\"email\":\"$LOGIN_EMAIL\",\"password\":\"$LOGIN_PASS\"}")

  if [[ "$LOGIN_HTTP" == "200" ]]; then
    ACCESS_TOKEN="$(jq -r '.access_token // empty' /tmp/login.json)"
    if [ -n "$ACCESS_TOKEN" ]; then
      echo -e "${GREEN}✅ Login erfolgreich.${NC}"
      break
    fi
  fi

  echo -e "${RED}❌ Login fehlgeschlagen (Versuch $TRY von $MAX_TRIES).${NC}"
  cat /tmp/login.json
  TRY=$((TRY+1))
done

if [ -z "$ACCESS_TOKEN" ]; then
  echo -e "${RED}❌ Zu viele fehlgeschlagene Login-Versuche. Abbruch.${NC}"
  exit 1
fi

# ======== Interaktive Eingaben mit Defaults ========
# macOS (BSD-date) ODER Linux (GNU-date) +30 Tage
DEFAULT_DUE="$(date -v+30d +%F 2>/dev/null || date -d '+30 days' +%F)"
TODAY="$(date +%F)"

# kleine Helper: read mit Default (zsh & bash)
read_default() {
  local __prompt="$1" __varname="$2" __default="$3" __input=""
  if [ -n "${ZSH_VERSION-}" ]; then
    read -r "${__varname}?${__prompt} [${__default}]: " || true
    eval "__input=\"\${${__varname}:-}\""
  else
    read -r -p "${__prompt} [${__default}]: " __input || true
  fi
  eval "$__varname=\"\${__input:-$__default}\""
}

echo -e "${CYAN}→ Angaben zum Projekt/Meilenstein${NC}"

# Status mit Default
read_default "📌 Status (angemeldet|geplant|laufend|abgeschlossen)" MILESTONE_STATUS "geplant"

# Team (optional)
read_default "👥 Team (frei Text, optional)" TEAM_NAMES ""

# Beschreibung (optional)
read_default "📝 Beschreibung (optional)" DESCRIPTION ""

# Fälligkeitsdatum (YYYY-MM-DD) mit Default +30 Tage
while true; do
  read_default "📅 Fälligkeitsdatum (YYYY-MM-DD)" DUE_DATE "$DEFAULT_DUE"
  if [[ "$DUE_DATE" =~ ^[0-9]{4}-[0-9]{2}-[0-9]{2}$ ]]; then break; fi
  echo -e "${RED}❌ Ungültiges Datum. Bitte Format YYYY-MM-DD verwenden.${NC}"
done

# Startdatum (heute) – falls du es setzen willst
START_DATE="$TODAY"


# ========= Projekt via Vercel-API -> Edge Function anlegen =========
echo -e "${CYAN}→ Supabase: projectnew (über Vercel -> Edge Function) …${NC}"

PAYLOAD="$(jq -n \
  --arg name   "$PROJECT_NAME" \
  --arg status "$MILESTONE_STATUS" \
  --arg start  "$START_DATE" \
  --arg team   "$TEAM_NAMES" \
  --arg due    "$DUE_DATE" \
  --arg desc   "$DESCRIPTION" \
  --arg repo   "${REPO_URL:-}" \
  '{
     name: $name,
     status: (($status | select(length>0)) // "geplant"),
     startdatum: $start,
     team:        ($team | select(length>0)),
     description: ($desc | select(length>0)),
     due_date:    ($due  | select(length>0)),
     repo_url:    ($repo | select(length>0))
   }')"

PR_HTTP=$(curl -s -o /tmp/projectnew.json -w "%{http_code}" \
  -X POST "$VERCEL_BASE/api/projectnew" \
  -H "Authorization: Bearer $ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d "$PAYLOAD")

if [[ ! "$PR_HTTP" =~ ^20[01]$ ]]; then
  echo -e "${RED}❌ Projektanlage fehlgeschlagen (HTTP $PR_HTTP):${NC}"
  cat /tmp/projectnew.json; exit 1
fi

echo -e "${GREEN}✅ Projekt '$PROJECT_NAME' erstellt & auf GitHub gepusht.${NC}"
echo -e "${GREEN}✅ Supabase Antwort:${NC}"; jq . /tmp/projectnew.json || cat /tmp/projectnew.json
echo -e "🔗 Repo: $REPO_URL"
